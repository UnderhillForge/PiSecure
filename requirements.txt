# Core dependencies for PiSecure
cryptography>=43.0.0
PyNaCl>=1.5.0
requests>=2.32.0
click>=8.1.0
rich>=14.0.0
textual>=0.50.0
python-dateutil>=2.9.0
psutil>=6.0.0

# Async HTTP client for remote monitoring and P2P networking
aiohttp>=3.10.0
aiodns>=3.2.0

# WebSocket support
websocket-client>=1.8.0
python-socketio[client]>=5.11.0

# Web Dashboard
Flask>=3.0.0
Flask-Cors>=5.0.0
Flask-Limiter>=3.7.0
Flask-SocketIO>=5.3.0

# Build tools for C++ components
pybind11>=2.13.0
cmake>=3.28.0
scikit-build-core>=0.9.0

# Database: SQLite3 is built-in to Python 3.7+

# Optional dependencies (uncomment as needed)
# ipfshttpclient>=0.8.0  # For IPFS integration
# RPi.GPIO>=0.7.0       # For GPIO access on Pi
# gpiozero>=2.0         # For GPIO abstraction
# tpm2-pytss>=1.2.0     # For TPM integration
# paho-mqtt>=1.6.1      # For MQTT support
# fastapi>=0.100.0      # For REST API
# uvicorn>=0.23.0       # For async server